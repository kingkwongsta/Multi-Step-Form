import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "hsl(213, 96%, 18%)",
    },
    seccondary: {
      main: "hsl(243, 100%, 62%)",
    },
  },
});

export default theme;
